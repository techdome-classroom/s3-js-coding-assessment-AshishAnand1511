/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
    let stack = [];

    const bktMap = {
        ")": "(",
        "}": "{",
        ']': "["
    };
    
};

module.exports = { isValid };


